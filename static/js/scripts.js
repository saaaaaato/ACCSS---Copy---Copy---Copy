// /static/js/scripts.js
function generate2FACode(secret) {
    // This is a placeholder function. You should use a library like otplib to generate real 2FA codes.
    return "123456"; // Example code
}

// Example usage
const secret = "YOUR_2FA_SECRET";
const code = generate2FACode(secret);
console.log(code);
